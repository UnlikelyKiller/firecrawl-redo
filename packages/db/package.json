{
  "name": "@crawlx/db",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "test": "vitest run --exclude src/__tests__/migration-smoke.test.ts",
    "test:migration-smoke": "vitest run src/__tests__/migration-smoke.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
